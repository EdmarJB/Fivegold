@model IEnumerable<FiveGold.Models.Mapeamento.ComentarioConhecimento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataComentario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TextoComentario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataComentario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TextoComentario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDComentario }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDComentario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDComentario })
        </td>
    </tr>
}

</table>
